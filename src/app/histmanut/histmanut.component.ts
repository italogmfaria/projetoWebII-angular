import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-histmanut',
  standalone: true,
  templateUrl: './histmanut.component.html',
  styleUrl: './histmanut.component.css',
  imports: [RouterModule]
})
export class HistmanutComponent {

}
