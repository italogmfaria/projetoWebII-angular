import { EnderecoOutputDTO } from './endereco-output-dto';

describe('EnderecoOutputDTO', () => {
  it('should create an instance', () => {
    expect(new EnderecoOutputDTO()).toBeTruthy();
  });
});
